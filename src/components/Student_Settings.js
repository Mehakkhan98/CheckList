import * as React from 'react';
import { View, Text, Button ,TouchableOpacity,Image, Dimensions,Alert} from 'react-native';

export default function Student_Notifications() {
    return (
      <View style={{ justifyContent: 'center', alignItems: 'center',backgroundColor: '#ffff' }}>
        <Text>Student Notifications Screen</Text>
      </View>
    );
  }